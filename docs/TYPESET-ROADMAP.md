# 조판 장기 로드맵 — 제보가 게이트가 되는 트랙

> 작성: 2026-08-18 · 공개 이슈 **#54**. 정본 위치: 현행 제품 방향은
> `docs/PRODUCT-DIRECTION-V3.md`(A~D축), 불변식 전문은 `docs/PRODUCT-DIRECTION.md` §4.
> 이 문서는 그 축들 사이에 걸쳐 있던 **조판 충실도**를 하나의 장기 트랙으로 묶는다.
> V3 "별 트랙 편집 품질"의 조판 반쪽이 이 문서다 — A~D축을 재정의하지 않는다.

---

## 0. 왜 장기 트랙인가 (실증 3개)

1. **#42 (외부 제보, 2026-08-14)** — 실물 공공 양식(.hwp)에서 ① 표 안 체크박스·내용 소실
   ② 2·3·5쪽 제목 표(머리말) 통째 소실 ③ 세로·가로 페이지 공존 미적용. 쪽수는 7==7인데
   내용이 비어 보인다 — **쪽수 게이트가 침묵하는 축**의 실물 사례다.
2. **PR #46이 보여준 구조적 긴장** — 세 구멍(폼 컨트롤 lift, 머리말 표 lift, HWP5 landscape
   스왑)을 수리하는 순간 `hwpx_rhwp_parity` 줄수 정합 테스트가 깨진다(우리 301 vs rhwp lift
   282). **수리가 정합 기준 자체의 재정의를 요구한다** — rhwp lift에 없는 것을 우리가 살리면
   "rhwp와 같음"은 더 이상 정답 정의가 아니다. 국소 패치로 닫을 수 없는 이유.
3. **rhwp 상류의 공표된 결론** (`docs/research/rhwp-landscape-2026-08.md`) — PI 93.75%에서
   *"조판 축은 국소 수정으로 더 오르지 않는다. 남은 국소 레버 전부 성공해도 상한 +1.8pp.
   다음 도약은 세로 공간 계산 재설계뿐"*. 그들의 정답지(한/글 COM, 회당 7~17시간)는 문단
   시작 쪽만 봐서 **줄 단위 어긋남이 침묵**하고, 우리 `layout-check`는 저장 lineseg 대조로
   정확히 그 축을 본다 — 우리가 구조적으로 유리한 지점이다.

## 1. 원칙 (위반 = 작업 실패)

- 게이트 하향 금지: benchmark 8==8 · benchmark1 18==18 · 줄바꿈 98.9%+ 유지. 정합 기준을
  **재정의**할 수는 있어도(아래 T0) 기존 게이트 수치를 느슨하게 풀지 않는다.
- LOCKSTEP: `place_doc` ↔ `NaiveLayout` 페이지 수 일치. rhwp는 vendored 무수정 + 파싱 전용.
- 제보 문서는 **개인정보 없이 구조만** 픽스처화한다(파일명·본문·해시 금지 — 제보 템플릿 규율
  그대로). 재현 불가 제보는 pending으로 정직하게 남긴다.
- 판정 함정 4종(rhwp #3556에서 배움): 오라클 통과 ≠ 무손실 · 착수 전 최신 main 확인 ·
  표본 1건을 전체로 일반화 금지 · 가설은 구현해서 기각(음성 결과도 결과).

## 2. 단계

| 단계 | 기간 | 목표 | 내용 |
|---|---|---|---|
| **T0** | 지금~1개월 | #42 세 구멍 착지 | PR #46 완주: 폼 컨트롤·머리말 표·landscape 스왑. **정합 게이트 재정의**가 선결 — rhwp-lift 동등성 테스트를 "rhwp가 lift하는 부분집합에서만 동등"으로 좁히고, 살려낸 요소는 자체 픽스처로 잠근다. 쪽수 무변(7==7) 확인 |
| **T1** | 1~3개월 | 제보→픽스처 파이프라인 | 레이아웃 문제 제보가 들어오면: 재현 → 최소 구조 픽스처(비식별) → 실패 테스트 → 수리 → 게이트 편입을 표준 절차로. 부위별 커버리지 지도(머리말/꼬리말·폼·가로세로 혼합·중첩 표 over-tall 클리핑·차트 밴드) 작성, 침묵 축 우선 |
| **T2** | 3~8개월 | 줄 단위 오라클 확장 | `layout-check` lineseg 대조를 셀 문단 너머 각주·다쪽 표 누적 높이로 확장. benchmark1 24-vs-18 페이지 인플레이션(중첩 표 시대의 기지 리스크) 해소. rhwp가 못 보는 축에서 수치로 앞선다 |
| **T3** | 8개월+ | 구조 재설계 (필요 시) | rhwp와 같은 결론에 도달하면(국소 수정 상한 실측) 세로 공간 계산 재설계를 검토. 전제는 rhwp #3582와 같다: 착수 전 3중 게이트(자체 게이트 + 제보 픽스처 코퍼스 + 시각 판정) 완비. **근거가 착수를 결정한다** — 단계가 아니라 |

V3와의 접점: T0~T1은 A축("기관 양식이 왕복하고 한컴이 연다")의 화면 반쪽을 받친다.
T2~T3은 별 트랙이다 — 벌크(B축)·생태계(C축) 일정에 조판 재설계를 끼워 넣지 않는다.

## 3. 다음 코딩 한 줄

**PR #46의 `hwpx_rhwp_parity`를 부분집합 동등으로 재정의하고, 살린 요소를 자체 픽스처로 잠근다.**
(실작업: Grok 4.6 — AGENTS.md §에이전트 역할 분담)

---

## 부록 — #42 제보자 답변 초안 (소유자가 게시)

> 자세한 제보 감사합니다. 첨부해 주신 재현 단서로 세 가지 원인을 특정했습니다:
> ① 1쪽 체크박스·내용 소실 = 폼 컨트롤(체크박스/입력란)을 화면 변환에서 떨어뜨리고 있었고,
> ② 2·3·5쪽 제목 표 소실 = 머리말(헤더) 영역의 표를 본문으로 올리지 않았으며,
> ③ 가로 페이지 미적용 = .hwp가 세로 규격+가로 플래그로 저장하는 것을 스왑하지 않았습니다.
>
> 세 건 모두 수리 브랜치(PR #46)에서 재현·수정됐지만, 머리말 표와 폼 컨트롤을 살리면
> 기존 조판 정합 테스트의 기준 자체를 다시 정의해야 해서 — 눈에 보이는 것보다 뿌리가 깊은
> 작업입니다. 그래서 이 부분을 일회성 패치가 아니라 조판 장기 로드맵(docs/TYPESET-ROADMAP.md)
> 으로 묶어 관리하기로 했습니다. 이 이슈는 세 구멍의 수리가 머지될 때 닫고, 이후 단계는
> 로드맵 문서에서 추적됩니다.
>
> 이런 실물 양식 제보가 저희 조판 커버리지 지도의 실제 재료가 됩니다. 다시 한번 감사합니다.
