# blog.autohwp.com 로드맵 — HWP 분석·업데이트를 검색 자산으로

> 작성: 2026-08-18 · 공개 이슈 **#54**. 메시지 규율은 `docs/launch/CONTENT-BRIEF.md`
> (허용/금지 주장 표 포함)가 정본이고, 이 문서는 그 위의 **배포 채널 하나**를 계획한다.

## 0. 왜 블로그인가

- **검색 표면이 얇다.** 제품 페이지(15 URL)만으로는 색인이 더디다 — Google 하위 URL
  pending, Brave 제출 5일차 0건(#33 증거). HWP 포맷 분석·조판 연구 같은 롱테일 질의는
  제품 페이지가 아니라 **글**이 받는다.
- **재료가 이미 레포에 있다.** `docs/research/rhwp-landscape-2026-08.md`(조판 오라클 비교),
  claw-hwp 분석, HWP5 재저장 여정(082), 중첩 표 분할, HWPUNIT/px 이원화 — 바깥에 없는
  1차 자료다. CONTENT-BRIEF §게시물 세트 1(한국어 장문 정본)도 "블로그 공용"을 이미 전제했다.
- **신뢰 서사.** "한계를 먼저 말한다" 원칙의 실행 채널 — 잘 안 되는 것의 원인 분석 글이
  곧 기여자 온보딩 문서가 된다.

## 1. 원칙

- 사용자 문서·문서 콘텐츠·비식별 실패 사례라도 **원문 캡처 게시 금지** — 구조 재현 픽스처와
  공개 샘플(sample-8p 등)만 쓴다.
- CONTENT-BRIEF 허용/금지 주장 표를 글마다 적용한다("한컴과 100% 호환" 금지 등).
- 코드·벤치 수치는 재현 커맨드와 함께 싣는다(주장을 재현할 수 있다 원칙).
- 제품명 표기: **오토한글(auto-hwp)**.

## 2. 단계

| 단계 | 목표 | 내용 |
|---|---|---|
| **B0 인프라** | blog.autohwp.com 라이브 | 모노레포 `apps/blog`(Next.js SSG, hwp-lab과 독립) 또는 hwp-lab `/blog` 라우트+서브도메인 rewrite 중 택1 — **별도 앱 권장**(빌드·배포 격리, hwp-lab 번들 무영향). 배포는 기존 규율 그대로: Vercel Git auto-deploy off, `vercel-deploy.yml` prebuilt 패턴 재사용. RSS·OG·sitemap·GA4(기존 property, 동의 게이트 동일) 포함. 마크다운 정본은 레포 안(`apps/blog/content/`) |
| **B1 창간** | 첫 3편 발행 | ① 조판 편: "쪽수가 맞아도 문서가 비어 보이는 이유" — #42 사례 + 오라클 침묵 축(TYPESET-ROADMAP §0 재료) ② 포맷 편: HWP5/HWPX 구조 분석(레코드·lineseg·재저장) ③ 업데이트 노트: 오픈소스 공개 후 한 달(이슈 유입→수리 사례 #19/#42/#48). 각 편 발행 시 sitemap 반영 + Search Console/Naver 수집 요청 |
| **B2 운영 리듬** | 지속 가능한 주기 | npm/제품 릴리스마다 업데이트 노트 1편(자동 초안: 릴리스 증거 문서→글 변환), 분석 글은 분기 1편 이상. Threads 시리즈와 크로스포스트(스레드=요약+링크, 블로그=정본). 색인 상태를 성장 체크포인트에 편입 |

## 3. 완료 기준 (B0 이슈화 시 수용 기준으로 승격)

- blog.autohwp.com 200 + RSS/sitemap 유효 + 다크모드·모바일 렌더 QA(브라우저 MCP)
- 제품 사이트와 상호 링크(랜딩 푸터 ↔ 블로그 헤더), robots 크롤 허용
- GA4 동의 전 요청 0 규율이 블로그에도 동일 적용(#30 계약)

## 4. 다음 한 줄

**B0 셸 선택(별도 앱 vs 라우트)을 이슈로 열고 창간 3편 목차를 확정한다.**
(기획·검수: Fable / 실작업: Grok 4.6 — AGENTS.md §에이전트 역할 분담)
