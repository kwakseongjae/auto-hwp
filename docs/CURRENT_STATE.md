# CURRENT STATE — 단일 복원 지점

> 새 세션·compact 후 **이 파일 하나만 읽으면 재개할 수 있어야 한다.**
> 갱신 시점: 작업 단위 완료 · 결정 확정 · 머지 직후 (보고보다 먼저). 프로토콜: `AGENTS.md` §세션 연속성.

- 기준 커밋: `d0f0a24` (`main` — 051=2dc92d3·052=d0f0a24 병합됨. GitHub: https://github.com/kwakseongjae/tf-hwp private)
- 갱신: 2026-07-10 · Claude(Fable 5) — R12 배치 A 완료 세션

## 지금 (현재 위치)
- 로드맵 기준: **R12 배치 A 완료** — 051(챗 구조편집)·052(자동저장/복구) done. 052 golden이 기존 엔진 갭 2건 격리 → **057 신설**(hwpx 표 앵커링 버그) + 054에 증상 기록(.hwp 무편집 왕복 8p→6p).
- 로드맵 정본: `docs/PRODUCT-DIRECTION-V2.md`(북극성 = 브라우저 프로덕션: 업로드→바이브+수동 편집→PDF) + 진행표 `docs/issues/README.md`(상태 진실은 git log — 복원 스크립트가 대조).
- 제품 현 수준: 웹(`apps/hwp-lab`)에서 업로드→수동+챗 편집→PDF/HWPX export가 전부 클라이언트사이드로 동작. 판정 = "강한 내부 데모/프라이빗 베타, GA 아님"(격차 5개가 이슈 051~056).

## 다음 (즉시 착수 가능)
1. 배치 B(053∥054∥057) 완료 보고 수신 → 순차 병합(각 게이트 재확인) → 통합 검증(wasm 재빌드
   포함 — AGENTS.md 함정 top6) → 푸시 → 이슈/상태 갱신.
- 착수 전 필독: `docs/PRODUCT-DIRECTION.md` §4(빌더 공통 계약) + 해당 이슈 파일의 "함정" 절.

## 막힘 / 대기 (없으면 "없음")
- 없음. (056 배포용 crypto는 "수요 확인" 게이트 — 미착수가 정상 상태)

## 진행 중 레인 (병렬 작업 시에만)
| 레인/ID | owner | 상태 | 다음 체크포인트 |
|---|---|---|---|
| 053 셀 캐럿 | 구현 에이전트(워크트리) | **구현 중** — P0(own-render 글리프 통일) 실측 후 진행, 불가면 중단·보고 | 커밋+게이트+해상률 수치 보고 → 병합 |
| 054 lift F2 | 구현 에이전트(워크트리) | **구현 중** — rhwp 소스 조사 표 → 실값 lift, 8p→6p 재현부터 | 커밋+게이트(8==8·18==18 필수)+오디트 보고 → 병합 |
| 057 표 앵커링 | 구현 에이전트(워크트리) | **수정 중** — Rust 재현 테스트 레드 → dirty 재방출 수정, verbatim 골든 불변 | 커밋+골든 무회귀 증빙 보고 → 병합 |
소유권: goldenRecovery.test.ts=057 단독 / hwp-rhwp=054 / hwp-mcp Intent·hwp-session·packages=053
