# 046 — R11-2: 아웃라인 패널 + 상태바 SDK 승격 (quick win S+S)

- 상태: **done** (3096bc0) · 우선순위: R11-P1 · 영역: packages/react(OutlinePanel/StatusBar 신규 + HwpWorkspace **레이아웃 컨테이너**) + editor-core(outline 질의)
- 병렬: 045(찾기/바꾸기 — HwpWorkspace의 키보드 이펙트·상단 바 영역 소유). 이 이슈는 **좌측
  사이드바·하단 상태바 컨테이너**만 만지고 keydown 이펙트/툴바/FindBar 영역을 만지지 마라.

## 근거 (TAURI-CONVERGENCE.md 감사표 — quick win 2종)
데스크톱엔 문서 아웃라인 패널(U4)과 상태바(U1)가 있고 SDK엔 없다. 둘 다 S 규모.

## 목표
- **아웃라인 패널**: 좌측 접이식 — 문서 구조(제목/표/페이지 단위 항목, 엔진이 주는 대로) 리스트,
  클릭 → 해당 페이지/블록으로 스크롤(035 줌과 정합 — 기존 스크롤 소스 재사용, 새 산술 금지).
  현재 화면 위치가 리스트에 하이라이트(스크롤 추적 — 가상화(037)의 visible과 무관하게 동작해야).
- **상태바**: 하단 얇은 바 — 현재 페이지/전체(스크롤 위치 기반), 선택 요약(앵커 라벨 재사용),
  편집모드 뱃지. 줌 %는 상단 툴바 소유이므로 중복 금지.
- 웹 lab에서 즉시 보임 + 신 셸(044) 데스크톱 자동 획득. 패널은 접기 상태 기억(로컬).

## 설계
1. 아웃라인 데이터: 데스크톱 doc_outline 커맨드가 기존재 — **wasm 파리티 실측**: 동등 인텐트/
   바인딩이 있는가? 없으면 crates/hwp-wasm에 additive 바인딩(hwp-session 파사드 경유, 018 null
   정책) 추가 — 이 경우 crates 접촉이므로 게이트 v2(8==8·18==18)+골든+wasm-safe를 워크트리에서
   직접 실행. 셀 내부 텍스트를 제목으로 오인하지 않게 데스크톱과 동일 소스 사용.
2. editor-core: outline() 질의 + 타입(양 어댑터 동형 DTO). node 테스트.
3. react: OutlinePanel(접이식, 클릭 네비, 현재 위치 추적 — rAF 스로틀 스크롤 리스너, 렌더 절제)
   + StatusBar + HwpWorkspace 레이아웃 배선(hw-body 좌측/하단 — 기존 캔버스/채팅 패널 구조 보존).
4. 테스트: vitest(아웃라인 렌더/클릭 스크롤 mock/현재 위치 하이라이트/접기 기억, 상태바 페이지
   추적/선택 요약), e2e 1개(열기 → 아웃라인 항목 클릭 → 스크롤 이동 assert + 상태바 페이지 갱신).
5. 무회귀: 037 가상화(패널 스크롤 추적이 placeholder 페이지에서도 정확), 030 dragPerf, 기존 전부.

## 수용 기준
- [ ] 아웃라인: 리스트+클릭 네비+현재 위치 추적+접기 기억 — vitest+e2e
- [ ] 상태바: 현재 페이지/전체·선택 요약·편집 뱃지 — vitest+e2e
- [ ] wasm 파리티(필요 시 additive 바인딩 + 게이트/골든/wasm-safe 증빙), 두 어댑터 동형
- [ ] 045 소유 영역 무접촉, 037/030 무회귀 포함 기존 스위트 그린, 언스테이지 0

## 함정
- 현재 페이지 추적을 IntersectionObserver visible set에 묶지 마라(037 내부 상태) — 스크롤 위치
  기반 독립 계산(placeholder 높이가 정확하므로 가능).
- 아웃라인 항목이 0개인 문서(제목 없음) → 페이지 목록 폴백(빈 패널 금지).
- e2e 전 빌드 순서 + **apps/hwp-lab `rm -rf .next` 필수**.
