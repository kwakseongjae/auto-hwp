{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "devUrl": "http://127.0.0.1:1421",
    "beforeDevCommand": {
      "cwd": "ui",
      "script": "pnpm exec vite --mode workspace --host 127.0.0.1 --port 1421 --strictPort"
    },
    "beforeBuildCommand": {
      "cwd": "ui",
      "script": "pnpm -C ../../../packages/editor-core build && pnpm -C ../../../packages/react build && pnpm exec tsc --noEmit && pnpm exec vite build --mode workspace"
    }
  }
}
