# tf-hwp — Claude Code 진입점

@AGENTS.md

> import 폴백(위 `@`가 동작하지 않는 환경): 세션 시작 시 `bash scripts/context_restore.sh` 실행
> (또는 `docs/CURRENT_STATE.md` 읽기) → 이슈 착수 전 `docs/PRODUCT-DIRECTION.md` §4 필독.
> 체크포인트/종료 규율과 불변식은 `AGENTS.md` 참조 — 두 파일의 정본은 AGENTS.md 하나다.
