{
  "name": "autohwp-demo-ai-proxy",
  "private": true,
  "version": "0.0.1",
  "description": "오토한글 정적 데모용 OpenRouter 프록시(Cloudflare Worker) — 키 보관 + 일일 한도 강제",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@auto-hwp/ai-protocol": "file:../../packages/ai-protocol"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "wrangler": "^3.90.0"
  }
}
