name = "autohwp-demo-ai"
main = "src/index.ts"
compatibility_date = "2026-07-01"

# 일일 카운터 저장소(IP별 + 전체). 배포 전 1회 생성 후 아래 id를 채운다:
#   wrangler kv namespace create RATELIMIT
[[kv_namespaces]]
binding = "RATELIMIT"
id = "77e71126e0534967932807bd012bc39a"

[vars]
# CORS 허용 오리진(정확히 일치, 콤마로 여러 개). Pages 프로젝트 페이지면 뒤에 경로 없이 오리진만.
ALLOWED_ORIGIN = "https://kwakseongjae.github.io"
MODEL = "z-ai/glm-5.2"
# 비용 상한의 실질 강제: GLM 5.2 요청당 ~$0.0038 → 1200건 ≈ $4.6(<$5). (Gemini Flash-Lite는 더 싸지만
# Cloudflare Worker 출구 리전을 구글이 지역 차단 — GLM으로 전환. 모델 바꾸면 이 캡도 재계산할 것.)
DAILY_CAP = "1200"
PER_IP_CAP = "20"
MAX_TOKENS = "1024"

# 키는 vars가 아니라 secret으로(레포/설정에 안 남는다):
#   wrangler secret put OPENROUTER_API_KEY
